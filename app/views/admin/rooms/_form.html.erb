<%= form_for([:admin, room], :html => {:class => "ui large form error"}) do |f| %>

    <%= render 'layouts/error_messages', object: room %>

    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <%= f.submit :class => "ui small blue submit button" %>

<% end %>
